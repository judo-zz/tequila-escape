# お前が飲め！ パーティモード 現行仕様書

最終確認日: 2026-04-30

対象ファイル:

- `party-mode.html`
- `party-mode.css`
- `party-mode.js`
- `assets/party_logo.png`
- `assets/yapi-top.png`
- `assets/koji.png`
- `assets/ryota.png`
- `assets/osho.png`
- `assets/nana.png`
- `assets/yapi-card.png`
- `assets/milk-card.png`

## 1. 概要

| 項目 | 内容 |
| --- | --- |
| タイトル | お前が飲め！ |
| モード | テキーラから逃げろ！ PARTY MODE |
| ジャンル | 山札めくり + 手札妨害カードゲーム |
| 勝利条件 | ゲーム終了時に酔いカウンターが最も少ない |
| 初期山札 | 41枚 |
| 初期手札 | 各プレイヤー4枚 |
| 周数 | 1周 / 2周 / 3周 |
| 対戦形式 | VS CPU / ローカル対人 |

山札を引き、テキーラを避ける。手札で山札を覗く、誰かに引かせる、順番を変える、次の被弾を重くするなどを行い、最後に一番しらふだったプレイヤーが勝つ。

## 2. 画面構成

### TOP画面

- `assets/party_logo.png` を大きく表示
- `assets/yapi-top.png` のキャラクターを中央に表示
- ゲーム紹介文を表示
- `対戦準備へ` ボタン
- `ゲームの説明` ボタン
- `カードモードへ` リンク

### 説明モーダル

TOPの `ゲームの説明` から開く。

- 勝利条件
- 1ターンの流れ
- 危険な山札カード
- 手札カード
- 特殊ルール

### セットアップ画面

共通項目:

- 対戦モード選択
- 周数選択
- ルール要約
- `乾杯スタート！`
- `カードモードへ`

VS CPU の項目:

- プレイヤー名
- 自分のキャラクター
- CPU人数: 2人 / 3人
- CPUキャラクター

ローカル対人の項目:

- プレイ人数: 2人 / 3人 / 4人
- 各プレイヤーの名前
- 各プレイヤーのキャラクター

### ゲーム画面

- HUD
  - 現在の周数
  - 山札残数
  - テキーラ系カード残数
  - 特殊状態バッジ
  - TOPボタン
- プレイヤー表示
  - アイコン
  - 名前
  - 酔いカウンター
  - 現在ターン / SKIP表示
- 中央ステージ
  - ターンバナー
  - 山札
  - イベントカード
  - 捨て札
  - 最新ログ最大4件
- 自分 / 現在手番者ステータス
  - アイコン
  - 名前
  - 酔いカウンター
  - 手札枚数
  - YOUR TURN / GUARD
- 手札エリア
- アクションバー
  - 山札を引く
  - ターン終了

### PASS画面

ローカル対人専用。ターンが移るたびに表示される。

- 次のプレイヤー名
- キャラクターアイコン
- `準備OK、めくる` ボタン
- 強制ドロー時は理由を表示

### リザルト画面

- 勝者 / 引き分けタイトル
- 総評
- ランキング
- 同じ設定でもう一度
- Xでポスト
- タイトルへ
- カードモードへ

## 3. 対戦モード

### VS CPU

人間1人 + CPU 2〜3人で遊ぶ。

CPUは自動で手札判断、山札ドロー、ターン終了を行う。CPUキャラクターによって判断の強さが変わる。

### ローカル対人

同じ端末を回して2〜4人で遊ぶ。

ターンが来るたびにPASS画面を挟み、次のプレイヤーが自分の手札だけを見られるようにする。

ローカル対人では全員が `isHuman: true` で、CPU行動は発生しない。

## 4. キャラクター

| ID | 表示名 | 画像 | CPU傾向 |
| --- | --- | --- | --- |
| `koji` | こーじ | `assets/koji.png` | 手強い |
| `ryota` | りょーた | `assets/ryota.png` | 手強い |
| `osho` | おしょー | `assets/osho.png` | 手強い |
| `nana` | なな | `assets/nana.png` | やさしめ |
| `yapi` | やぴ | `assets/yapi-card.png` | やさしめ |
| `milk` | みるく | `assets/milk-card.png` | やさしめ |

VS CPUでは、自分に選んだキャラクターはCPUに選べない。

ローカル対人では、同じキャラクターは複数プレイヤーに選べない。

### CPU難易度

| 値 | hard | easy |
| --- | ---: | ---: |
| 山札を見る使用率 | 0.95 | 0.52 |
| 危険札回避判断率 | 0.96 | 0.62 |
| 全員乾杯回避判断率 | 0.82 | 0.48 |
| 妨害カード使用率 | 0.58 | 0.26 |
| 帰宅方向逆です妨害率 | 0.42 | 0.22 |
| お前が飲め最適対象率 | 0.88 | 0.62 |
| セーフ後にもう1枚引く率 | 0.24 | 0.58 |

男性キャラは `hard`、女性キャラは `easy`。

## 5. 山札カード

初期山札は41枚。

| ID | 表示名 | 枚数 | 効果 |
| --- | --- | ---: | --- |
| `safe` | セーフ | 27 | 何も起きない |
| `tequila` | テキーラ | 10 | 引いた人の酔い+1 |
| `kanpai` | 全員集合！乾杯！ | 3 | 全員が山札を1枚ずつ引く |
| `tequila_party` | 全員強制テキーラ乾杯 | 1 | 全員がテキーラ被弾。全員手札補充 |

### セーフ

ログとイベントカードにセーフ表示。酔いは増えない。

### テキーラ

基本は酔い+1。被弾したプレイヤーは増えた酔いの数だけ手札を補充する。

`倍プッシュ` と `回避スタック` がある場合は増加量が変わる。

### 全員集合！乾杯！

全員が山札を1枚ずつ引く。

乾杯中にさらに `kanpai` を引いた場合、そのカードはセーフ扱いになり、乾杯は連鎖しない。

### 全員強制テキーラ乾杯

全員がテキーラ被弾する。

`飲みません宣言` を使っているプレイヤーは個別に回避でき、回避成功時に手札+1。

`倍プッシュ` 中なら全員の被弾が基本+2になる。

`回避スタック` は最初に実際に被弾したプレイヤーへ適用される。

## 6. 山札周回

山札が空になった状態で次のドローを試みると、1周終了。

| 条件 | 処理 |
| --- | --- |
| `deckCycle < maxCycles` | 捨て札をシャッフルし、テキーラ2枚を追加して次の周へ |
| 周回継続時 | 全員の手札+1 |
| `deckCycle >= maxCycles` | `gameOverPending = true` になり、ターン終了でリザルトへ |

例: 2周設定なら、1周目の山札を引き切った後、テキーラ2枚追加で2周目を開始する。2周目を引き切るとゲーム終了待ちになる。

## 7. 手札アクションカード

アクションカードは合計32枚。

| ID | 表示名 | 枚数 | 効果 | 使用後ターン終了 |
| --- | --- | ---: | --- | :---: |
| `reverse` | 帰宅方向逆です | 4 | ターン順を逆回りにする | はい |
| `force` | とりあえず一杯 | 5 | 次のプレイヤーを強制ドロー対象にする | はい |
| `target` | お前が飲め | 5 | 選んだ相手に山札を引かせる | いいえ |
| `double` | 倍プッシュだ……！ | 4 | 次のテキーラ被弾時、酔い増加を2倍 | いいえ |
| `peek` | 嫌な予感 | 5 | 山札の一番上を見る | いいえ |
| `skip` | スキップ | 4 | 次のプレイヤーを飛ばす | はい |
| `dodge` | 回避 | 3 | 次のテキーラ被弾者への酔い増加を重くする | はい |
| `guard` | 飲みません宣言 | 2 | 次の自分のテキーラ系被弾を無効化し、手札+1 | いいえ |

### 帰宅方向逆です

`direction` を反転する。使用後すぐターン終了。

### とりあえず一杯

使用時点の次プレイヤーIDを `forceDrawTarget` に保存する。使用後すぐターン終了。

その後に順番が変わっても、強制ドロー対象は変わらない。

### お前が飲め

対象プレイヤーに山札を1枚引かせる。

VS CPUで対象がCPUなら即時解決。対象が人間なら `FORCED_DRAW` に入る。

ローカル対人では対象者のPASS画面に移り、対象者が引いたあと、使用者のターンに戻る。使用者はドロー義務を果たした扱いになる。

### 倍プッシュだ……！

`doublePushActive = true` にする。

次に実際にテキーラ被弾が発生した時、酔い増加量を2倍にして、その後解除する。

### 嫌な予感

山札の一番上を見る。人間プレイヤーはピークオーバーレイで確認できる。

CPUはこのカードを使うと、見た山札トップに応じて行動を変える。

### スキップ

次プレイヤーの `skipped` を立てる。使用後すぐターン終了。

スキップされたプレイヤーはターン開始時に飛ばされる。

### 回避

`dodgeStack` を+1する。最大3。使用後すぐターン終了。

次に実際にテキーラ被弾したプレイヤーへ加算され、その後解除される。

### 飲みません宣言

自分の `noDrinkGuard = true` にする。

次に自分が `tequila` または `tequila_party` を引いた / 受けた場合、それを無効化し、手札を1枚補充する。

テキーラ系以外のカードを引いた場合は空振りで消費される。

## 8. ターン進行

### 通常ターン

1. 手札カードを任意で使う
2. 山札を1枚引く
3. 山札を引いた後も、残り手札を使える
4. `ターン終了` で次のプレイヤーへ

山札を引くまでは `ターン終了` は押せない。

### 強制ドロー

`とりあえず一杯` または `お前が飲め` の対象になった時に発生する。

強制ドロー中は手札を使えず、山札を引く必要がある。

### スキップ解決

ターン移動時、次プレイヤーに `skipped` が立っていたら、そのプレイヤーのフラグを解除してさらに次へ進む。

全員スキップ状態の異常ケースでは、全員のスキップを解除して続行する。

## 9. CPU行動

CPUターンでは以下を自動判断する。

1. 強制ドロー対象なら山札を引いてターン終了
2. `嫌な予感` を持っていれば、確率で使って山札を見る
3. 山札トップが危険なら、確率で回避・押しつけを狙う
4. 山札トップが `全員集合！乾杯！` なら、確率で先送りや回避を狙う
5. 確率で妨害カードを使う
6. 最終的に山札を引く
7. セーフなら、性格に応じた確率でもう1枚引く

危険札に対してはおおむね以下の優先:

1. `倍プッシュ` + `お前が飲め`
2. `お前が飲め`
3. `飲みません宣言`
4. `回避`
5. 山札を引く

## 10. 状態変数

| 変数 | 内容 |
| --- | --- |
| `state.players` | プレイヤー配列 |
| `state.actionPool` | 手札アクション山札 |
| `state.deck` | 山札 |
| `state.deckDiscard` | 山札捨て札 |
| `state.actionDiscard` | アクションカード捨て札 |
| `state.turnOrder` | プレイヤー順 |
| `state.currentTurnIdx` | 現在の `turnOrder` インデックス |
| `state.direction` | 1または-1 |
| `state.deckCycle` | 終了した周数 |
| `state.maxCycles` | 設定した最大周数 |
| `state.gameOverPending` | 山札周回終了後のゲーム終了待ち |
| `state.doublePushActive` | 倍プッシュ中 |
| `state.dodgeStack` | 回避スタック |
| `state.forceDrawTarget` | とりあえず一杯の対象ID |
| `state.forcedDrawPending` | 強制ドロー待機情報 |
| `state.targetPendingFrom` | PvPでお前が飲め使用者のターン位置 |
| `state.hasDrawnThisTurn` | 現ターンで山札を引いたか |
| `state.lastEvent` | 中央イベントカード表示 |
| `state.phase` | 現在フェーズ |
| `state.log` | 行動ログ |
| `state.gameMode` | `vs-cpu` または `pvp` |

### phase

| phase | 内容 |
| --- | --- |
| `SETUP` | セットアップ前 |
| `PLAYER_TURN` | 人間 / 現手番者の通常ターン |
| `CPU_TURN` | CPUターン |
| `RESOLVING` | 処理中 |
| `FORCED_DRAW` | 強制ドロー待ち |
| `PASS` | ローカル対人の端末受け渡し |
| `RESULT` | 結果画面 |

## 11. リザルト

酔いカウンターが最も少ないプレイヤーを勝者にする。

同率最少が複数いる場合は引き分け。

VS CPUでは人間プレイヤーの順位を総評に反映する。

ローカル対人では最も飲んだプレイヤーを総評に出す。

### X投稿

`Xでポスト` は `https://twitter.com/intent/tweet` へ遷移する。

投稿文には以下を含める。

- モード名
- 周数
- 人数
- 勝者
- VS CPUでは自分の順位
- ローカル対人では全員の最終酔い

ハッシュタグは `テキーラから逃げろ`。

## 12. 実装メモ

主要関数:

| 関数 | 役割 |
| --- | --- |
| `buildDeck()` | 山札生成 |
| `buildActionPool()` | アクションカード山札生成 |
| `initState()` | VS CPU初期化 |
| `initStatePvp()` | ローカル対人初期化 |
| `drawFromDeck()` | 山札ドロー。空なら周回処理 |
| `onDeckCycleEnd()` | 周回終了処理 |
| `resolveDrawCard()` | 山札カード解決 |
| `applyAction()` | 手札カード解決 |
| `endTurn()` | ターン終了 |
| `resolveSkips()` | スキップ解決 |
| `runCpuTurn()` | CPUターン開始 |
| `runCpuDecideAfterPeek()` | CPU判断 |
| `renderAll()` | 全UI再描画 |
| `renderHud()` | HUD再描画 |
| `renderPlayers()` | プレイヤー表示 |
| `renderStage()` | 中央ステージ・ログ・PASS表示 |
| `renderHand()` | 手札表示 |
| `renderActionBar()` | 操作ボタン表示 |
| `showResultScreen()` | 結果表示 |
| `buildPartyResultPostText()` | X投稿文生成 |

## 13. 注意事項

- 実際の飲酒を推奨するゲームではなく、演出上の「テキーラ」「酔い」表現として扱う。
- ローカル対人では同じ端末を回すため、PASS画面で手札の覗き見を防ぐ。
- `bg.jpg` は現行では使わず、背景は `assets/bg.png` を使う。
- アセット名はGitHub Pagesで大文字小文字が区別されるため、実ファイル名どおりに参照する。
